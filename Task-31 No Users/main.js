"use strict";
let username = ["admin", "Mukesh", "Rajesh", "Kapil", "Jai"];
if (username.length === 0) {
    console.log('We need to find some users!');
}
else {
    username = [];
    console.log('All user Names have been removed. ' + username.length);
}
